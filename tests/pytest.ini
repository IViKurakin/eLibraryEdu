[pytest]
DJANGO_SETTINGS_MODULE = elibrary_project.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra -q --strict-markers --disable-warnings
testpaths = .
markers =
    slow: marks tests as slow
    login: тесты с авторизованным пользователем